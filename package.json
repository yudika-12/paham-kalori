{
  "name": "paham-kalori",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "db:push": "npm run db:push -w frontend",
    "generate": "npm run generate -w frontend"
  }
}